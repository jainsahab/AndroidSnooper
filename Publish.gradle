apply plugin: 'maven-publish'

afterEvaluate {
    publishing {
        publications {
            aar(MavenPublication) {
                from components.release
            }
        }
        repositories {
            mavenLocal()
        }
    }

}
